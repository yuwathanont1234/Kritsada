# Guardian (ผู้พิทักษ์) — client environment
# Same Supabase project as the watch app (Guardian uses guardian_-prefixed tables).
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-side secret (set via `supabase secrets set`, NOT in this client file):
#   ANTHROPIC_API_KEY   — used by the guardian-analyze Edge Function
