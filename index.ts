import { registerRootComponent } from 'expo';
import App from './App';

// Register the main App component with the Expo framework.
// This handles safe area environments and native system loading.
registerRootComponent(App);
